# Page — React 샘플 (react-page)

이 폴더는 기존 `index_v4.html`을 보존한 채, React + Tailwind로 새 페이지를 개발하기 위한 템플릿입니다.

빠른 시작:

```bash
cd react-page
npm install
npm run dev
```

- `npm run dev` 실행 후 브라우저에서 `http://localhost:5173`을 엽니다.
- 기존 원본 파일은 프로젝트 루트의 `index_v4.html`로 남아있습니다. 새 컴포넌트를 점진적으로 옮기세요.
